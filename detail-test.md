# API测试系统设计方案

## 用户需求

1. **REST风格Endpoint参数嵌入测试**：在测试时如何将参数嵌入到API请求中，进行有效的测试。
2. **认证管理**：不同API可能需要不同的认证方式，需要一个认证管理系统，在请求API测试时可以选择认证方式，执行认证时使用配置的认证获取token或其他方式，根据认证的配置将token或其他信息嵌入到header中。
3. **API测试依赖管理**：API测试可能需要有依赖关系，如新建、修改、查看、删除操作形成闭环，希望API有依赖关系后，新建的id可以在修改时作为参数，查看修改或创建的信息，删除则使用创建的id进行删除，实现测试闭环。

## 现有系统分析

### 系统架构
- **前端**：基于Web的用户界面
- **后端**：FastAPI构建的API管理系统
- **数据库**：使用SQLAlchemy ORM，支持Alembic迁移

### 现有功能
1. **API管理**：管理API、Endpoint、System等资源
2. **测试管理**：创建、更新、删除测试用例
3. **测试执行**：执行API或Endpoint的测试用例
4. **测试结果**：获取测试结果、测试批次、测试趋势
5. **认证**：基于JWT的用户认证系统

### 现有测试功能
- **环境支持**：dev、uat、prod环境配置
- **请求配置**：支持设置请求头和请求体
- **验证规则**：支持基本的响应验证
- **测试执行**：支持批量执行和单步执行
- **结果分析**：支持测试趋势分析

## 设计目标

基于用户需求和现有系统功能，设计一个增强的API测试系统，重点实现闭环测试功能，同时保持与现有系统的兼容性。

## 1. 系统架构

### 1.1 核心模块
- **API管理模块**：管理API、Endpoint、System等资源（现有）
- **测试套件模块**：在API级别创建和管理测试套件（新增，替代原测试管理模块）
- **测试执行模块**：执行API测试并处理依赖关系（现有，增强）
- **结果分析模块**：分析测试结果并生成报告（现有）
- **参数传递模块**：管理测试用例之间的参数传递（新增）
- **认证管理模块**：管理不同Endpoint的认证配置（新增）

## 2. REST风格Endpoint参数嵌入设计

### 2.1 参数类型支持
- **路径参数**：嵌入到URL路径中的参数，如`/api/users/{id}`（现有系统已支持基本路径参数替换）
- **查询参数**：URL末尾的查询字符串，如`/api/users?page=1&limit=10`
- **请求体参数**：POST/PUT请求中的JSON/表单数据（现有系统已支持）
- **Header参数**：HTTP请求头中的参数（现有系统已支持）

### 2.2 参数嵌入方式
- **静态参数**：直接输入固定值（现有系统已支持）
- **动态参数**：从其他测试步骤获取值（新增）
- **变量引用**：使用`${variable_name}`语法引用变量（新增）

### 2.3 实现方案
- **参数解析器**：解析测试配置中的参数，识别变量引用
- **变量存储**：存储测试执行过程中提取的变量
- **参数替换**：在测试执行前替换URL、请求体和Header中的变量
- **与现有系统集成**：扩展现有`EndpointTest`模型，添加参数配置字段

## 3. 认证管理设计

### 3.1 认证类型支持
- **Bearer Token**：JWT等令牌认证，支持配置token获取URL、账号密码等参数
- **Basic Auth**：用户名密码认证，支持配置用户名和密码
- **API Key**：API密钥认证，支持配置API Key和Header名称
- **OAuth 2.0**：授权码、客户端凭证等流程，支持配置完整的OAuth参数
- **自定义认证**：支持自定义认证逻辑，可配置任意参数

### 3.2 认证配置管理
- **认证模板**：预定义常用认证方式的模板
- **凭证安全存储**：加密存储敏感凭证信息
- **认证有效期管理**：自动刷新过期的认证令牌
- **环境隔离**：不同环境使用不同的认证配置
- **认证参数配置**：
  - **Bearer Token**：token获取URL、请求方法、请求体参数、响应中token路径、Header名称
  - **Basic Auth**：用户名、密码、Header格式
  - **API Key**：API Key值、Header名称或查询参数名称
  - **OAuth 2.0**：授权URL、token URL、客户端ID、客户端密钥、作用域等

### 3.3 认证执行流程
1. 选择认证方式（从预定义模板或自定义）
2. 配置认证参数（如获取token的URL、账号密码、Header变量名等）
3. 执行认证获取token或其他凭证
4. 将凭证自动嵌入到请求头或其他指定位置
5. 验证认证是否成功
6. 缓存认证结果，避免重复认证

### 3.4 与Endpoint集成
- **Endpoint级认证**：为每个Endpoint配置认证方式
- **认证继承**：Endpoint可继承API或系统级的认证配置
- **认证优先级**：Endpoint级认证 > API级认证 > 系统级认证

### 3.5 实现方案
- **认证模型**：创建`AuthConfig`模型，存储认证配置
- **认证服务**：实现`AuthService`，处理认证逻辑
- **与现有系统集成**：扩展现有`Endpoint`模型，添加认证配置关联
- **凭证安全**：使用加密存储敏感凭证信息

## 4. 测试套件设计

### 4.1 测试套件架构
- **API级别创建**：在API级别创建测试套件，替代原Endpoint级别的测试用例
- **测试用例组成**：每个测试套件包含多个测试用例，每个测试用例关联一个Endpoint
- **环境隔离**：为不同环境创建不同的测试套件
- **依赖关系**：测试用例之间可以定义依赖关系，支持参数传递

### 4.2 测试用例设计
- **Endpoint选择**：每个测试用例选择一个Endpoint
- **参数配置**：为测试用例配置参数，支持静态参数和动态参数
- **变量提取**：配置从响应中提取变量的规则
- **验证规则**：配置响应验证规则

### 4.3 参数传递机制
- **变量提取**：从测试响应中提取数据并存储为变量
- **变量引用**：在后续测试中引用已存储的变量
- **变量作用域**：支持全局、测试套件和测试用例级别的变量
- **变量类型**：支持字符串、数字、JSON对象等类型
- **变量提取规则**：使用JSONPath或XPath从响应中提取数据

### 4.4 闭环测试实现
1. **创建资源**：POST /api/resources，提取响应中的`id`并存储为变量
2. **查看资源**：GET /api/resources/{id}，使用步骤1的`id`变量
3. **修改资源**：PUT /api/resources/{id}，使用步骤1的`id`变量，并验证修改结果
4. **删除资源**：DELETE /api/resources/{id}，使用步骤1的`id`变量
5. **验证删除**：GET /api/resources/{id}，使用步骤1的`id`变量，验证资源已删除

### 4.5 参数Mapping配置
- **URL参数映射**：将提取的变量映射到URL路径参数或查询参数
- **请求体参数映射**：将提取的变量映射到请求体中的特定字段
- **Header参数映射**：将提取的变量映射到请求头中的特定字段
- **Mapping规则**：支持直接映射、JSONPath映射、正则表达式映射等

### 4.6 测试套件管理
- **套件创建**：在API级别创建测试套件，选择环境
- **用例管理**：向测试套件添加测试用例，选择Endpoint
- **依赖配置**：配置测试用例之间的依赖关系
- **变量管理**：管理测试套件级别的变量
- **执行顺序**：定义测试用例的执行顺序

### 4.7 实现方案
- **测试套件模型**：创建`TestSuite`模型，管理测试流程，关联API和环境
- **测试用例模型**：创建`TestCase`模型，关联测试套件和Endpoint
- **测试依赖模型**：创建`TestDependency`模型，存储测试用例之间的依赖关系
- **变量模型**：创建`TestVariable`模型，存储测试过程中的变量
- **测试执行服务**：扩展现有`TestService`，支持测试套件执行和参数传递
- **与现有系统集成**：扩展现有`TestBatch`模型，支持测试套件执行



## 5. 测试执行与结果管理

### 5.1 测试执行模式
- **套件执行**：执行包含依赖关系的测试套件，实现闭环测试（新增）
- **环境选择**：执行测试时选择环境，使用对应环境的测试套件

### 5.2 执行流程
1. **选择API**：选择要测试的API
2. **选择环境**：选择测试环境（dev、uat、prod）
3. **选择测试套件**：选择要执行的测试套件
4. **执行测试**：系统按顺序执行测试套件中的测试用例
5. **处理依赖**：处理测试用例之间的依赖关系，传递参数
6. **认证处理**：根据每个Endpoint的认证配置获取认证信息并嵌入到请求中

### 5.3 结果分析
- **响应状态码验证**：验证HTTP状态码是否符合预期
- **响应内容验证**：验证响应数据是否符合预期
- **变量提取结果**：记录从响应中提取的变量值
- **依赖执行分析**：分析依赖测试的执行情况
- **认证执行分析**：分析认证过程的执行情况

### 5.4 报告生成
- **测试套件报告**：生成详细的测试套件执行报告，包括依赖关系分析
- **API测试报告**：生成API级别的测试报告

### 5.5 与现有系统集成
- **测试执行服务**：重写`TestService`，支持测试套件执行和参数传递
- **测试结果存储**：扩展现有`TestResult`模型，存储变量提取结果和认证执行结果
- **测试批次管理**：扩展现有`TestBatch`模型，支持测试套件执行
- **API集成**：添加新的API端点，支持测试套件管理和执行

## 6. 技术实现建议

### 6.1 后端技术
- **语言**：Python（现有系统已使用）
- **API框架**：FastAPI（现有系统已使用）
- **数据库**：SQLAlchemy ORM（现有系统已使用）
- **异步处理**：asyncio（现有系统已使用）
- **HTTP客户端**：httpx（现有系统已使用）

### 6.3 存储方案
- **测试配置**：数据库（现有系统已使用）
- **测试结果**：数据库（现有系统已使用）
- **变量存储**：数据库
- **依赖关系**：数据库

### 6.4 新增依赖
- **JSONPath**：用于从响应中提取变量
- **pydantic**：用于数据验证（现有系统已使用）



## 8. 总结

### 核心功能
1. **API级别测试套件**：在API级别创建测试套件，替代原Endpoint级别的测试用例
2. **测试用例管理**：每个测试套件包含多个测试用例，每个测试用例关联一个Endpoint
3. **参数传递机制**：支持从测试响应中提取变量，并在后续测试中引用这些变量
4. **闭环测试实现**：实现创建、查看、修改、删除的完整测试闭环
5. **参数Mapping配置**：支持将提取的变量映射到URL、请求体和Header参数
6. **环境隔离**：为不同环境创建不同的测试套件
7. **认证管理**：为每个Endpoint配置认证方式，系统自动处理认证过程

### 技术实现
- **数据库模型**：创建TestSuite、TestCase、TestDependency和TestVariable模型
- **测试执行服务**：重写TestService，支持测试套件执行和参数传递
- **认证服务**：实现AuthService，处理认证逻辑
- **与现有系统集成**：扩展现有TestBatch和TestResult模型，支持测试套件执行

### 实施步骤
1. **创建数据库迁移文件**：使用Alembic创建TestSuite、TestCase、TestDependency和TestVariable表
2. **实现测试套件**：创建TestSuite模型和相关服务
3. **实现测试用例**：创建TestCase模型和相关服务
4. **实现参数传递**：创建TestVariable模型和变量提取功能
5. **实现认证管理**：创建AuthConfig模型和AuthService
6. **重写测试执行服务**：修改TestService，支持测试套件执行和参数传递
7. **编写测试**：编写单元测试和集成测试
8. **系统集成**：集成所有模块，确保系统正常运行