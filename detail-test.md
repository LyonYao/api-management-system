# API测试系统设计方案

## 用户需求

1. **REST风格Endpoint参数嵌入测试**：在测试时如何将参数嵌入到API请求中，进行有效的测试。
2. **认证管理**：不同API可能需要不同的认证方式，需要一个认证管理系统，在请求API测试时可以选择认证方式，执行认证时使用配置的认证获取token或其他方式，根据认证的配置将token或其他信息嵌入到header中。
3. **API测试依赖管理**：API测试可能需要有依赖关系，如新建、修改、查看、删除操作形成闭环，希望API有依赖关系后，新建的id可以在修改时作为参数，查看修改或创建的信息，删除则使用创建的id进行删除，实现测试闭环。

## 现有系统分析

### 系统架构
- **后端**：FastAPI构建的API管理系统
- **数据库**：使用SQLAlchemy ORM，支持Alembic迁移

### 现有功能
1. **API管理**：管理API、Endpoint、System等资源
2. **测试管理**：创建、更新、删除测试用例
3. **测试执行**：执行API或Endpoint的测试用例
4. **测试结果**：获取测试结果、测试批次、测试趋势

### 现有测试功能
- **环境支持**：dev、uat、prod环境配置
- **请求配置**：支持设置请求头和请求体
- **验证规则**：支持基本的响应验证
- **测试执行**：支持批量执行和单步执行

## 设计目标

基于用户需求和现有系统功能，设计一个精简的API测试系统，实现测试套件、参数传递和认证管理功能，同时保持与现有系统的兼容性。

## 1. 系统架构

### 1.1 核心模块
- **API管理模块**：管理API、Endpoint、System等资源（现有）
- **测试套件模块**：在API级别创建和管理测试套件（新增）
- **测试执行模块**：执行API测试并处理依赖关系（现有，增强）
- **认证管理模块**：管理不同Endpoint的认证配置（新增）

## 2. 参数传递设计

### 2.1 变量提取
- **响应提取**：从测试响应中提取数据并存储为变量
- **提取规则**：使用JSONPath从响应中提取数据
- **变量命名**：为提取的变量指定名称，用于后续引用

### 2.2 变量引用
- **语法**：使用`${variable_name}`语法在测试配置中引用变量
- **支持位置**：URL路径参数、查询参数、请求体参数、Header参数

### 2.3 实现方案
- **变量存储**：使用内存存储测试执行过程中的变量
- **参数替换**：在测试执行前替换配置中的变量引用
- **与现有系统集成**：扩展测试执行服务，添加变量提取和替换功能

## 3. 认证管理设计

### 3.1 认证类型支持
- **Bearer Token**：JWT等令牌认证
- **Basic Auth**：用户名密码认证
- **API Key**：API密钥认证

### 3.2 认证配置
- **配置项**：
  - **Bearer Token**：token获取URL、请求方法、请求体参数、响应中token路径、Header名称
  - **Basic Auth**：用户名、密码
  - **API Key**：API Key值、Header名称
- **环境隔离**：不同环境使用不同的认证配置

### 3.3 认证执行流程
1. 为Endpoint配置认证方式和参数
2. 执行测试时，系统根据认证配置获取凭证
3. 将凭证自动嵌入到请求头中
4. 执行API请求

### 3.4 实现方案
- **认证模型**：创建`AuthConfig`模型，存储认证配置
- **认证服务**：实现`AuthService`，处理认证逻辑
- **与现有系统集成**：扩展现有`Endpoint`模型，添加认证配置关联

## 4. 测试套件设计

### 4.1 测试套件架构
- **API级别创建**：在API级别创建测试套件
- **测试用例组成**：每个测试套件包含多个测试用例，每个测试用例关联一个Endpoint
- **环境隔离**：为不同环境创建不同的测试套件

### 4.2 测试用例设计
- **Endpoint选择**：每个测试用例选择一个Endpoint
- **参数配置**：为测试用例配置参数，支持静态参数和变量引用
- **变量提取**：配置从响应中提取变量的规则
- **验证规则**：配置响应验证规则

### 4.3 闭环测试实现
1. **创建资源**：POST /api/resources，提取响应中的`id`并存储为变量
2. **查看资源**：GET /api/resources/${id}，使用步骤1的`id`变量
3. **修改资源**：PUT /api/resources/${id}，使用步骤1的`id`变量，并验证修改结果
4. **删除资源**：DELETE /api/resources/${id}，使用步骤1的`id`变量
5. **验证删除**：GET /api/resources/${id}，使用步骤1的`id`变量，验证资源已删除

### 4.4 测试套件管理
- **套件创建**：在API级别创建测试套件，选择环境
- **用例管理**：向测试套件添加测试用例，选择Endpoint
- **执行顺序**：定义测试用例的执行顺序

### 4.5 实现方案
- **测试套件模型**：创建`TestSuite`模型，管理测试流程，关联API和环境
- **测试用例模型**：创建`TestCase`模型，关联测试套件和Endpoint
- **测试执行服务**：重写`TestService`，支持测试套件执行和参数传递
- **与现有系统集成**：扩展现有`TestBatch`模型，支持测试套件执行



## 5. 测试执行

### 5.1 执行流程
1. **选择API**：选择要测试的API
2. **选择环境**：选择测试环境（dev、uat、prod）
3. **选择测试套件**：选择要执行的测试套件
4. **执行测试**：系统按顺序执行测试套件中的测试用例
5. **参数传递**：处理测试用例之间的参数传递
6. **认证处理**：根据每个Endpoint的认证配置获取认证信息并嵌入到请求中

### 5.2 结果管理
- **测试结果**：记录测试执行结果，包括响应状态码、响应内容等
- **变量提取结果**：记录从响应中提取的变量值
- **报告生成**：生成测试套件执行报告

### 5.3 实现方案
- **测试执行服务**：重写`TestService`，支持测试套件执行和参数传递
- **测试结果存储**：扩展现有`TestResult`模型，存储变量提取结果
- **API集成**：添加新的API端点，支持测试套件管理和执行

## 6. 技术实现

### 6.1 后端技术
- **语言**：Python（现有系统已使用）
- **API框架**：FastAPI（现有系统已使用）
- **数据库**：SQLAlchemy ORM（现有系统已使用）
- **HTTP客户端**：httpx（现有系统已使用）

### 6.2 存储方案
- **测试配置**：数据库（现有系统已使用）
- **测试结果**：数据库（现有系统已使用）

### 6.3 新增依赖
- **JSONPath**：用于从响应中提取变量



## 7. 总结

### 核心功能
1. **API级别测试套件**：在API级别创建测试套件，包含多个测试用例
2. **测试用例管理**：每个测试用例关联一个Endpoint，支持参数配置和变量提取
3. **参数传递机制**：支持从测试响应中提取变量，并在后续测试中引用这些变量
4. **闭环测试实现**：实现创建、查看、修改、删除的完整测试闭环
5. **认证管理**：为每个Endpoint配置认证方式，系统自动处理认证过程
6. **环境隔离**：为不同环境创建不同的测试套件

### 技术实现
- **数据库模型**：创建TestSuite和TestCase模型
- **认证模型**：创建AuthConfig模型
- **测试执行服务**：重写TestService，支持测试套件执行和参数传递
- **认证服务**：实现AuthService，处理认证逻辑
- **与现有系统集成**：扩展现有TestResult模型，支持测试套件执行

### 实施步骤
1. **创建数据库迁移文件**：使用Alembic创建TestSuite、TestCase和AuthConfig表
2. **实现测试套件**：创建TestSuite模型和相关服务
3. **实现测试用例**：创建TestCase模型和相关服务
4. **实现认证管理**：创建AuthConfig模型和AuthService
5. **重写测试执行服务**：修改TestService，支持测试套件执行和参数传递
6. **系统集成**：集成所有模块，确保系统正常运行